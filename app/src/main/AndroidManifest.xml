<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="org.o7planning.mpt1">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.MPT1">
        <activity
            android:name=".menu.MainMenuActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".AddThemeActivity"></activity>
        <activity android:name=".viewBaseData.CollectActivity"></activity>
        <activity android:name=".viewBaseData.ThemeActivity"></activity>
        <activity android:name=".AddQuestionActivity"></activity>
        <activity android:name=".viewBaseData.QuestionActivity"></activity>
        <activity android:name=".AddCollectActivity"></activity>
        <activity android:name=".menu.StartTest"></activity>
    </application>

</manifest>